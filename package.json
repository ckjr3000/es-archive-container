{
  "name": "es-archive-container",
  "version": "1.0.0",
  "description": "This website was created to contain the archive of [Huddersfield University's Electric Spring Festival]('https://electricspring.co.uk/').",
  "main": ".eleventy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "npx @11ty/eleventy --serve",
    "build": "npx eleventy && node generateSearchIndex.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
